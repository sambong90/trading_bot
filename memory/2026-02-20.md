2026-02-20 21:04 KST - Session notes

- Live trading activity: Multiple LiveExecutor order attempts were executed after explicit LIVE confirmation. Several buy attempts for KRW-ATOM made; some were accepted and partially/fully filled, others were rejected due to UnderMinTotalBid or InsufficientFunds.
  - Observed persisted orders in DB (orders table) with various order_id values and status 'submitted'. Example recent rows: id=7 order_id=1771617616740 buy None 0.0 submitted 2026-02-20 20:00:16.659104 ... id=1 order_id=test123 buy 1000.0 0.5 submitted 2026-02-20 19:00:00
  - Upbit API returned cancellation/filled records for some UUIDs (e.g. a7977da4-77f7-4558-8c73-b9e426c9cc01 — cancel but executed_volume=1.4645577).

- Immediate reconciliation actions performed:
  - Queried Upbit /v1/orders (states: wait/done/cancel). Found no 'wait' (open) orders for our account at time of check; found done and cancel records. 
  - Persisted order attempts and fetched balances.
  - Confirmed account balances after activity: KRW ~4,997.88403524; ATOM 1.4645577; other assets unchanged.

- Scheduler and auto_summary control:
  - Located running scheduler_service.py process earlier and terminated it.
  - User crontab contained an auto_summary entry (*/5 * * * * ...auto_summary.py). Multiple attempts were made to remove it. Final state: crontab cleared (no crontab for user), auto_summary disabled.

- Errors / issues observed during reconciliation run:
  - Upbit API: UnderMinTotalBid and InsufficientFundsBid observed during live attempts.
  - Runtime: AttributeError previously due to missing LiveExecutor._persist_order (fixed earlier).
  - During bulk sync attempt, SQLite rejected string timestamps: "SQLite DateTime type only accepts Python datetime and date objects as input." Need to convert API timestamp strings to datetime objects before insert.

- Next planned actions (short-term):
  - Fix sync script to parse timestamps into datetime objects, paginate orders, and commit in batches.
  - Re-run reconciliation to update orders table and create Trade records for executed fills.
  - Optionally disable LIVE_MODE (or remove LIVE_CONFIRM) until LiveExecutor is hardened.
  - Add order-reconciliation job that runs periodically (or on-demand) and emits Telegram alerts for fills/failures.

- Safety decisions: LiveExecutor requires UPBIT_ACCESS_KEY/UPBIT_SECRET_KEY + LIVE_MODE=1 + LIVE_CONFIRM="I CONFIRM LIVE" gating. For now, recommend keeping LIVE_MODE disabled until code reviewed and reconciliation completed.

Notes recorded by assistant (삼봉) during session.

