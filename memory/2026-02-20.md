2026-02-20

- 사용자와의 합의 및 아이덴티티
  - 사용자(삼봉)는 나를 "삼봉"이라 부르기로 했음.
  - 사용자는 친근한 톤(친구처럼)으로 대화하기를 선호함.

- 주요 목표
  - Upbit에서 작동하는 코인 자동매매 프로그램 개발이 우선 목표.
  - 초기 자본: KRW 100,000.
  - 거래소: Upbit.
  - 프로그램은 LLM을 전략 설계/튜닝에 활용하며, 자동으로 학습·적응하도록 설계함.

- 초기 구현 및 선택사항
  - 언어/스택: Python, pyupbit, pandas, vectorized/backtest 스켈레톤, Flask+Plotly 대시보드.
  - 기본 전략: SMA 크로스(초기: short=10, long=50, timeframe=60m). 추가로 Momentum/RSI/ML 전략 제안됨.
  - 리스크 규칙(권장 기본): position_pct=10%, stop_loss=1.5%, trailing_stop=2%, daily_loss_limit=3%, max_drawdown=15%.
  - 페이퍼트레이딩 우선(권고): 페이퍼 2-4주 후 실거래 소액 병행.

- 현재 진행상황(2026-02-20 11:11 KST)
  - Telegram 연동 성공 및 테스트 메시지 수신 확인됨.
  - 가상환경(.venv) 및 의존성 설치 완료(requests, pyupbit, pandas, plotly, apscheduler 등).
  - 기본 스켈레톤 파일 생성됨: data.py, strategy.py, backtest.py, executor.py, tuner.py, main.py, monitor.py 등.
  - 단위 테스트(데이터 fetch → 시그널 → 백테스트) 통과.
  - 소규모 그리드 튜닝 및 5-fold walk-forward 실행 완료, 결과 logs에 저장됨.
  - 리스크 규칙 적용 백테스트 실행 완료, 결과 logs에 저장됨.
  - 대시보드(Flask + Plotly) 구현 및 로컬 실행 확인, PNG 시각화 생성됨.
  - 자동요약(5분 주기) 스케줄러(APScheduler) 설치 및 서비스 구성됨.
  - auto_summary 새 한국어 카드 포맷 생성 및 동작 확인(수동 실행으로 Telegram 전송 확인).

- 결정/합의된 운영 정책
  - 라이브 모드 활성화는 --live 플래그 + UPBIT_ACCESS_KEY/SECRET env + 수동 확인("I CONFIRM LIVE") 조건 모두 필요.
  - 초기 실거래는 소액 병행(권장)으로 시작하고 데이터 수집 후 점진적 확대.
  - 모든 민감정보는 trading_bot/.env 또는 OS 키체인에 보관(절대 공개하지 않음). .env.example 제공함.

- 단기 작업 계획(우선순위)
  1) A: data.py 개선(타임존 정리, 재시도/backoff) — 완료.
  2) B: backtest 개선(에퀴티 커브, stop/trail 적용 시점 명확화, CAGR/MDD/Sharpe 계산) — 진행 중.
  3) C: executor 개선(주문 유효성 검사, 부분체결 시뮬, 재시도, 로그 표준화) — 예정.
  4) 튜닝 강화 및 walk-forward 결과 기준으로 최적 파라미터 도출.
  5) 페이퍼 장기 실행(2-4주) 및 소액 실거래 병행으로 실데이터 수집.

- 기타 메모
  - 사용자 요청으로 모든 로그를 trading_bot/logs/에 저장하도록 구성함.
  - 대시보드에 현재 진행 카드 통합 예정.
  - 자동요약 포맷을 한국어 카드형으로 변경했고, state_updater로 모듈들이 자동 갱신하도록 통합 중.

- 11:29-11:30 조치(스케줄러/auto_summary 관련)
  - 문제: 스케줄러가 구버전 코드를 메모리에서 유지해 자동 전송 시 구버전(파일명 목록)으로 전송되는 현상 발생.
  - 조치: 스케줄러를 수정해 각 실행 시 trading_bot/tasks/auto_summary.py를 별도 하위프로세스로 실행하도록 변경함(항상 최신 파일 사용).
  - 조치: 기존 scheduler_service.py 프로세스들을 종료하고 최신 코드로 재시작함.
  - 검증: 수동 실행 및 로그 확인으로 새 한글 카드 포맷이 정상 생성됨(summary_auto.txt mtime 11:27 및 11:30). 스케줄러가 별도 프로세스 방식으로 실행되며 이후 자동 전송은 새 포맷으로 전송될 예정.


(이 항목은 MEMORY에 저장된 장기 기억으로 사용자가 향후 요구할 때 참조함.)
